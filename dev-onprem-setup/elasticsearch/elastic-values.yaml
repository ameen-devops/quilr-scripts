clusterName: "quilr-elasticsearch"

security:
  enabled: true
  elasticPassword: "QU#ILRe$%LLastic@457"
  tls:
    restEncryption: true
    autoGenerated: true

master:
  replicaCount: 1
  masterOnly: false
  resources:
    requests:
      memory: "2Gi"
      cpu: "500m"
    limits:
      memory: "4Gi"
      cpu: "750m"
  persistence:
    enabled: true
    existingClaim: elasticsearch-master-pvc
    storageClass: manual

data:
  replicaCount: 0

ingest:
  replicaCount: 0

coordinating:
  replicaCount: 0
